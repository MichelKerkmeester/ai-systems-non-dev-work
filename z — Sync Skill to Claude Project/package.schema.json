{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://barter.internal/ai-system-sync/package.schema.json",
  "title": "AI System Sync package manifest (claude-project.sync.json)",
  "description": "Hand-authored mapping and naming-decision contract for one registered system's derived claude.ai Project package. lib/manifest.cjs enforces this shape at runtime with a hand-rolled validator (the fleet is dependency-free, so no JSON Schema engine is installed); this file is the documented reference the validator is written against, not an executable schema.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "id",
    "skillRoot",
    "skillId",
    "knowledgeRoot",
    "kernel",
    "sourceCoverage",
    "mirrors",
    "contractInputs",
    "expectedKnowledgeCount"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1,
      "description": "Manifest schema version. Starts at 1 per the design authority."
    },
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Must match one registry.json system id exactly."
    },
    "skillRoot": {
      "type": "string",
      "minLength": 1,
      "description": "Current sk-* folder name, repo-root-relative. Must match registry.json's skillRoot for this id."
    },
    "skillId": {
      "type": "string",
      "minLength": 1,
      "description": "Stable invocation or graph identity for this skill (e.g. the description.json \"name\" or graph-metadata.json \"skill_id\"), used by the graph-target and retired-token checks."
    },
    "knowledgeRoot": {
      "const": "claude project/knowledge",
      "description": "Fixed location of the Project Knowledge mirror folder, package-root-relative."
    },
    "kernel": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "version", "alignedSkillVersion"],
      "properties": {
        "path": {
          "const": "claude project/Custom Instructions.md",
          "description": "Fixed location of the hand-synthesized kernel, package-root-relative."
        },
        "version": {
          "type": "string",
          "minLength": 1,
          "description": "Human-selected Project kernel version (e.g. \"1.3.0\")."
        },
        "alignedSkillVersion": {
          "type": "string",
          "minLength": 1,
          "description": "Human-selected claim of which sk-* skill version this kernel is aligned to."
        }
      }
    },
    "sourceCoverage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["include"],
      "properties": {
        "include": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 },
          "description": "Authoritative source paths (package-root-relative), each either a file or a folder covering every file recursively beneath it - there is no glob syntax, naming a folder is how \"cover everything in it\" is expressed. Every file covered here must be mapped by an entry in \"mirrors\", or the run blocks as UNMAPPED_SOURCE."
        },
        "exclude": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Changelogs, archives, benchmarks, or other intentional coverage omissions."
        }
      }
    },
    "mirrors": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["source", "target", "sourceVersion", "projectVersion"],
        "properties": {
          "source": {
            "type": "string",
            "minLength": 1,
            "description": "Normalized package-root-relative authoritative source path under skillRoot (e.g. \"sk-product-owner/SKILL.md\"); absolute paths and traversal are rejected."
          },
          "target": {
            "type": "string",
            "minLength": 1,
            "description": "Exact Project Knowledge filename inside knowledgeRoot. Directory components, absolute paths and traversal are rejected; naming remains an explicit human decision."
          },
          "sourceVersion": {
            "type": "string",
            "description": "Version pin as it exists in the authoritative source."
          },
          "projectVersion": {
            "type": "string",
            "description": "Version pin as it appears in the mirror's filename/frontmatter."
          }
        }
      }
    },
    "contractInputs": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "description": "Package-root-relative paths whose combined bytes make up the ai-system-contract-v1 digest. A change to any of these requires a recorded review-kernel decision."
    },
    "validators": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "command", "cwd"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "command": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "Argument vector, e.g. [\"python3\", \"benchmark/_gates/gates.py\", \"check\"]. Never a shell string, so no shell-quoting ambiguity."
          },
          "cwd": {
            "type": "string",
            "minLength": 1,
            "description": "Normalized package-root-relative directory to run the command from; absolute paths and traversal are rejected."
          }
        }
      }
    },
    "retiredNames": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Old folder, mirror, or version tokens forbidden outside declared history (scanExcludes)."
    },
    "scanExcludes": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Historical changelog/archive paths (package-root-relative) exempt from the retired-token scan."
    },
    "expectedKnowledgeCount": {
      "type": "integer",
      "minimum": 0,
      "description": "Must equal the number of declared mirror entries."
    },
    "derivationExceptions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "reason", "renderer", "projectFrontmatter"],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Exact mirror target filename (knowledgeRoot-relative) rendered by the declared deterministic renderer."
          },
          "reason": {
            "type": "string",
            "minLength": 1,
            "description": "Documented reason this mirror needs deterministic transformation instead of a byte copy."
          },
          "renderer": {
            "const": "project-skill-mirror-v1",
            "description": "Deterministic renderer that adds Project retrieval frontmatter and retargets mapped references/assets to sibling Project Knowledge filenames."
          },
          "projectFrontmatter": {
            "type": "object",
            "additionalProperties": false,
            "required": ["contextType", "importanceTier", "triggerPhrases"],
            "properties": {
              "contextType": { "type": "string", "minLength": 1 },
              "importanceTier": { "type": "string", "minLength": 1 },
              "triggerPhrases": {
                "type": "array",
                "minItems": 1,
                "items": { "type": "string", "minLength": 1 }
              }
            }
          }
        }
      },
      "description": "Declared, reviewed deterministic transformations of the plain byte-copy mirror rule. Sync renders and parity-checks every listed target; no declared mirror remains hand-maintained."
    },
    "generatedRegions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["target", "sections"],
        "properties": {
          "target": {
            "type": "string",
            "minLength": 1,
            "description": "Fixed package-root-relative host file: \"SYNC.md\" or \"claude project/README.md\"."
          },
          "sections": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "Registered renderer names used in BEGIN/END markers: INVENTORY, CHECKSUMS or SMOKE_VERSION_PINS."
          }
        }
      }
    }
  }
}
